# Babel extraction config for this module's own translation catalog.
# Extract with:  pybabel extract -F babel.cfg -o translations/messages.pot .
# then init/update de and compile:  pybabel compile -d translations
[python: **.py]
[jinja2: **/templates/**.html]
