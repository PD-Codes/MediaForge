name: Docker

# Replaces docker-check.yaml + trivy-scan.yaml, which each built their own copy
# of the image -- two full Docker builds per push and per pull request. This
# builds once and scans that image.
#
# Uses pull_request (not pull_request_target) on purpose: it builds untrusted
# fork PR code and needs neither repo secrets nor a write-scoped GITHUB_TOKEN,
# so the default read-only, secret-less fork token is enough.
on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'entrypoint.sh'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'src/**'
      - '.dockerignore'
      - '.github/workflows/docker.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'entrypoint.sh'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'src/**'
      - '.dockerignore'
      - '.github/workflows/docker.yaml'
  schedule:
    # A vulnerability in a base-image package appears without anyone touching
    # the repository, so the scan also runs on its own once a week.
    - cron: '0 4 * * 1'

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

# Least privilege: without this block the job runs with whatever the repository
# default is, which on older repos is read AND write for the whole token -- in a
# job that builds code straight out of the checkout.
permissions:
  contents: read

jobs:
  build-and-scan:
    name: Build and scan image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      # Turns "are there new security fixes for the base image?" into a value the
      # build cache can key on. `apt-get -s upgrade` is a dry run, so this only
      # reads package lists. Stable hash -> the apt layer stays cached; a fixed
      # package landing in debian-security changes it and forces the rebuild.
      - name: Probe base image for pending security upgrades
        id: apt
        run: |
          echo "hash=$(docker run --rm python:3.13-slim sh -c \
            'apt-get update -qq >/dev/null 2>&1 && apt-get -s upgrade 2>/dev/null | grep ^Inst | sort | sha256sum' \
            | cut -c1-16)" >> "$GITHUB_OUTPUT"

      - name: Build image
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          push: false
          load: true                  # keep it in the local daemon for Trivy
          tags: mediaforge:ci
          build-args: |
            APT_REFRESH=${{ steps.apt.outputs.hash }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@c07df6fec6fa692e6fd1200d50aaa1fdd66f03c8 # master @ 2026-07
        with:
          image-ref: 'mediaforge:ci'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          scanners: 'vuln'
          severity: 'CRITICAL,HIGH'
